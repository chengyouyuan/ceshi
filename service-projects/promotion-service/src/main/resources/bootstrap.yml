server:
  port: 3500

spring:
  profiles:
    active: dev
  application:
    name: b2c-promotion-service
  cloud:
    config:
      label: promotion
      discovery:
        enabled: true
        service-id: b2c-config-server

eureka:
  instance:
    prefer-ip-address: true
