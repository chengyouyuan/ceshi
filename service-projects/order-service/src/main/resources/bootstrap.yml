server:
  port: 3101

spring:
  profiles:
    active: dev
  application:
    name: order-service
  cloud:
    config:
      label: order
      discovery:
        enabled: true
        service-id: config-server

eureka:
  instance:
    prefer-ip-address: true
