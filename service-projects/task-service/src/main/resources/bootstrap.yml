server:
  port: 3800

spring:
  profiles:
    active: dev
  application:
    name: b2c-task-service
  cloud:
    config:
      label: task
      discovery:
        enabled: true
        service-id: b2c-config-server

eureka:
  instance:
    prefer-ip-address: true
