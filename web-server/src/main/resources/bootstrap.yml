server:
  port: 3050

spring:
  profiles:
    active: dev
  application:
    name: web-server
  cloud:
    config:
      label: web
      discovery:
        enabled: true
        service-id: config-server

eureka:
  instance:
    prefer-ip-address: true

