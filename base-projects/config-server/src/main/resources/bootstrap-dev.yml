spring:
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/conf/
  boot:
    admin:
      client:
        url: http://localhost:1111/admin/

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/