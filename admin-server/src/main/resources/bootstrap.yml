server:
  port: 3050

spring:
  profiles:
    active: dev
  application:
    name: b2c-admin-server
  cloud:
    config:
      label: admin
      discovery:
        enabled: true
        service-id: b2c-config-server

eureka:
  instance:
    prefer-ip-address: true
