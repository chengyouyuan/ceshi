server:
  port: 3101

spring:
  profiles:
    active: dev
  application:
    name: b2c-system-service
  cloud:
    config:
      label: system
      discovery:
        enabled: true
        service-id: b2c-config-server

eureka:
  instance:
    prefer-ip-address: true
