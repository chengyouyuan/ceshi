eureka:
  instance:
    hostname: registry2.huistore
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://registry1.huistore:${server.port}/eureka/