server:
  port: 3100

spring:
  profiles:
    active: dev
  application:
    name: b2c-customer-service
  cloud:
    config:
      label: customer
      discovery:
        enabled: true
        service-id: b2c-config-server

eureka:
  instance:
    prefer-ip-address: true

logging:
  level:
     com.winhxd.b2c.customer.dao : debug
