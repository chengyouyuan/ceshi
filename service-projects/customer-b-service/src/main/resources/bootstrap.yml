server:
  port: 3101

spring:
  profiles:
    active: dev
  application:
    name: customer-b-service
  cloud:
    config:
      label: customer-b
      discovery:
        enabled: true
        service-id: config-server

eureka:
  instance:
    prefer-ip-address: true
