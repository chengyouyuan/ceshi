server:
  port: 3101

spring:
  profiles:
    active: dev
  application:
    name: system-service
  cloud:
    config:
      label: system
      discovery:
        enabled: true
        service-id: config-server

eureka:
  instance:
    prefer-ip-address: true
