server:
  port: 3101

spring:
  profiles:
    active: dev
  application:
    name: b2c-pay-service
  cloud:
    config:
      label: pay
      discovery:
        enabled: true
        service-id: b2c-config-server

eureka:
  instance:
    prefer-ip-address: true
