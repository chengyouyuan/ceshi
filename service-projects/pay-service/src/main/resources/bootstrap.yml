server:
  port: 3101

spring:
  profiles:
    active: dev
  application:
    name: pay-service
  cloud:
    config:
      label: pay
      discovery:
        enabled: true
        service-id: config-server

eureka:
  instance:
    prefer-ip-address: true
