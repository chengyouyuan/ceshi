server:
  port: 3200

spring:
  profiles:
    active: dev
  application:
    name: b2c-message-service
  cloud:
    config:
      label: message
      discovery:
        enabled: true
        service-id: b2c-config-server