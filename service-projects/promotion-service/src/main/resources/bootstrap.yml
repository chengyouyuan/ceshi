server:
  port: 3101

spring:
  profiles:
    active: dev
  application:
    name: promotion-service
  cloud:
    config:
      label: promotion
      discovery:
        enabled: true
        service-id: config-server

eureka:
  instance:
    prefer-ip-address: true
