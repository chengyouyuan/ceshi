server:
  port: 3101

spring:
  profiles:
    active: dev
  application:
    name: b2c-order-service
  cloud:
    config:
      label: order
      discovery:
        enabled: true
        service-id: b2c-config-server

eureka:
  instance:
    prefer-ip-address: true
